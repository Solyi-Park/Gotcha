export type Address = {
  id?: string;
  title: string | null;
  userId: string;
  postCode: string;
  address: string;
  addDetail: string;
  default: boolean;
  name: string;
  contact: string | null;
};
